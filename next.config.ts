import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['blog-frontend.envato.com',"images.pexels.com","images.remotePatterns","randomuser.me"],
  },
  
};

export default nextConfig;
